  <b>Results submitted by:</b>
  <%= link_to race_result.user.name, user_path(race_result.user) %>
  <%= link_to_if have_perm('edit_results', league.id),
      '(edit)', edit_race_result_path(race_result) %>

<table width="100%">
  <tr>
    <th>Position</th>
    <th>User</th>
    <th>Status</th>
  </tr>
<% race_result.rows.each do |row| %>
  <tr>
    <td><%= row.position %></td>
    <td><%= link_to row.user.name, user_path(row.user) %></td>
    <td><%= row.status %></td>
  </td>
<% end %>
</table>
<p/> 

