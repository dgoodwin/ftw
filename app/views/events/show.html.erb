<p class="notice"><%= notice %></p>

<p>
  <b>Time:</b>
  <%= @event.time %>
</p>

<p>
  <b>Season:</b>
  <%= link_to @event.season.name, season_path(@event.season) %>
</p>

<p>
<b>Races:</b>
<ul>
  <% @event.races.each do |race| %>
    <li><%= link_to race, race_path(race) %></li>
  <% end %>
</ul>
</p>


<%= link_to 'Schedule Event', :controller => "events", :action => "schedule", :id => @event %><p/>


<%= link_to 'Edit', edit_event_path(@event) %> |
<%= link_to 'Back', events_path %>
